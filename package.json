{
  "name": "nila",
  "version": "3.0.0",
  "devDependencies": {
    "lerna": "^3.14.1"
  },
  "scripts": {
    "install:all": "lerna exec -- npm install",
    "build": "lerna run build"
  },
  "workspaces": [
    "packages/*"
  ]
}
